# Set to true ONLY during local development when Roble is unavailable.
# Never set this to true in a production build — the local datasource
# contains hardcoded credentials and bypass tokens.
EXPO_PUBLIC_USE_LOCAL_AUTH=false
